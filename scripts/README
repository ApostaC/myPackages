==============================================================
1) First, change HPLdir in Make.inc to the directory of xhpl. 

2) Type command
	make

3) Under ${HPLdir}/scripts
   Modify config.dat

4) Under ${HPLdir}
   Use run.sh to start
   Type 
	./run.sh -h
   to get help
==============================================================
